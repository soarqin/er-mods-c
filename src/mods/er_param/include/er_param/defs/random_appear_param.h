#pragma once

typedef struct er_random_appear_param_s {
    /* 抽選スロットID0
     *   スロットが当選した場合に出現するか
     * Random Slot [0]
     *   Will it appear if the slot is won? */
    unsigned char slot0:1;

    /* 抽選スロットID1
     *   スロットが当選した場合に出現するか
     * Random Slot [1]
     *   Will it appear if the slot is won? */
    unsigned char slot1:1;

    /* 抽選スロットID2
     *   スロットが当選した場合に出現するか
     * Random Slot [2]
     *   Will it appear if the slot is won? */
    unsigned char slot2:1;

    /* 抽選スロットID3
     *   スロットが当選した場合に出現するか
     * Random Slot [3]
     *   Will it appear if the slot is won? */
    unsigned char slot3:1;

    /* 抽選スロットID4
     *   スロットが当選した場合に出現するか
     * Random Slot [4]
     *   Will it appear if the slot is won? */
    unsigned char slot4:1;

    /* 抽選スロットID5
     *   スロットが当選した場合に出現するか
     * Random Slot [5]
     *   Will it appear if the slot is won? */
    unsigned char slot5:1;

    /* 抽選スロットID6
     *   スロットが当選した場合に出現するか
     * Random Slot [6]
     *   Will it appear if the slot is won? */
    unsigned char slot6:1;

    /* 抽選スロットID7
     *   スロットが当選した場合に出現するか
     * Random Slot [7]
     *   Will it appear if the slot is won? */
    unsigned char slot7:1;

    /* 抽選スロットID8
     *   スロットが当選した場合に出現するか
     * Random Slot [8]
     *   Will it appear if the slot is won? */
    unsigned char slot8:1;

    /* 抽選スロットID9
     *   スロットが当選した場合に出現するか
     * Random Slot [9]
     *   Will it appear if the slot is won? */
    unsigned char slot9:1;

    /* 抽選スロットID10
     *   スロットが当選した場合に出現するか
     * Random Slot [10]
     *   Will it appear if the slot is won? */
    unsigned char slot10:1;

    /* 抽選スロットID11
     *   スロットが当選した場合に出現するか
     * Random Slot [11]
     *   Will it appear if the slot is won? */
    unsigned char slot11:1;

    /* 抽選スロットID12
     *   スロットが当選した場合に出現するか
     * Random Slot [12]
     *   Will it appear if the slot is won? */
    unsigned char slot12:1;

    /* 抽選スロットID13
     *   スロットが当選した場合に出現するか
     * Random Slot [13]
     *   Will it appear if the slot is won? */
    unsigned char slot13:1;

    /* 抽選スロットID14
     *   スロットが当選した場合に出現するか
     * Random Slot [14]
     *   Will it appear if the slot is won? */
    unsigned char slot14:1;

    /* 抽選スロットID15
     *   スロットが当選した場合に出現するか
     * Random Slot [15]
     *   Will it appear if the slot is won? */
    unsigned char slot15:1;

    /* 抽選スロットID16
     *   スロットが当選した場合に出現するか
     * Random Slot [16]
     *   Will it appear if the slot is won? */
    unsigned char slot16:1;

    /* 抽選スロットID17
     *   スロットが当選した場合に出現するか
     * Random Slot [17]
     *   Will it appear if the slot is won? */
    unsigned char slot17:1;

    /* 抽選スロットID18
     *   スロットが当選した場合に出現するか
     * Random Slot [18]
     *   Will it appear if the slot is won? */
    unsigned char slot18:1;

    /* 抽選スロットID19
     *   スロットが当選した場合に出現するか
     * Random Slot [19]
     *   Will it appear if the slot is won? */
    unsigned char slot19:1;

    /* 抽選スロットID20
     *   スロットが当選した場合に出現するか
     * Random Slot [20]
     *   Will it appear if the slot is won? */
    unsigned char slot20:1;

    /* 抽選スロットID21
     *   スロットが当選した場合に出現するか
     * Random Slot [21]
     *   Will it appear if the slot is won? */
    unsigned char slot21:1;

    /* 抽選スロットID22
     *   スロットが当選した場合に出現するか
     * Random Slot [22]
     *   Will it appear if the slot is won? */
    unsigned char slot22:1;

    /* 抽選スロットID23
     *   スロットが当選した場合に出現するか
     * Random Slot [23]
     *   Will it appear if the slot is won? */
    unsigned char slot23:1;

    /* 抽選スロットID24
     *   スロットが当選した場合に出現するか
     * Random Slot [24]
     *   Will it appear if the slot is won? */
    unsigned char slot24:1;

    /* 抽選スロットID25
     *   スロットが当選した場合に出現するか
     * Random Slot [25]
     *   Will it appear if the slot is won? */
    unsigned char slot25:1;

    /* 抽選スロットID26
     *   スロットが当選した場合に出現するか
     * Random Slot [26]
     *   Will it appear if the slot is won? */
    unsigned char slot26:1;

    /* 抽選スロットID27
     *   スロットが当選した場合に出現するか
     * Random Slot [27]
     *   Will it appear if the slot is won? */
    unsigned char slot27:1;

    /* 抽選スロットID28
     *   スロットが当選した場合に出現するか
     * Random Slot [28]
     *   Will it appear if the slot is won? */
    unsigned char slot28:1;

    /* 抽選スロットID29
     *   スロットが当選した場合に出現するか
     * Random Slot [29]
     *   Will it appear if the slot is won? */
    unsigned char slot29:1;

    /* 抽選スロットID30
     *   スロットが当選した場合に出現するか
     * Random Slot [30]
     *   Will it appear if the slot is won? */
    unsigned char slot30:1;

    /* 抽選スロットID31
     *   スロットが当選した場合に出現するか
     * Random Slot [31]
     *   Will it appear if the slot is won? */
    unsigned char slot31:1;

    /* 抽選スロットID32
     *   スロットが当選した場合に出現するか
     * Random Slot [32]
     *   Will it appear if the slot is won? */
    unsigned char slot32:1;

    /* 抽選スロットID33
     *   スロットが当選した場合に出現するか
     * Random Slot [33]
     *   Will it appear if the slot is won? */
    unsigned char slot33:1;

    /* 抽選スロットID34
     *   スロットが当選した場合に出現するか
     * Random Slot [34]
     *   Will it appear if the slot is won? */
    unsigned char slot34:1;

    /* 抽選スロットID35
     *   スロットが当選した場合に出現するか
     * Random Slot [35]
     *   Will it appear if the slot is won? */
    unsigned char slot35:1;

    /* 抽選スロットID36
     *   スロットが当選した場合に出現するか
     * Random Slot [36]
     *   Will it appear if the slot is won? */
    unsigned char slot36:1;

    /* 抽選スロットID37
     *   スロットが当選した場合に出現するか
     * Random Slot [37]
     *   Will it appear if the slot is won? */
    unsigned char slot37:1;

    /* 抽選スロットID38
     *   スロットが当選した場合に出現するか
     * Random Slot [38]
     *   Will it appear if the slot is won? */
    unsigned char slot38:1;

    /* 抽選スロットID39
     *   スロットが当選した場合に出現するか
     * Random Slot [39]
     *   Will it appear if the slot is won? */
    unsigned char slot39:1;

    /* 抽選スロットID40
     *   スロットが当選した場合に出現するか
     * Random Slot [40]
     *   Will it appear if the slot is won? */
    unsigned char slot40:1;

    /* 抽選スロットID41
     *   スロットが当選した場合に出現するか
     * Random Slot [41]
     *   Will it appear if the slot is won? */
    unsigned char slot41:1;

    /* 抽選スロットID42
     *   スロットが当選した場合に出現するか
     * Random Slot [42]
     *   Will it appear if the slot is won? */
    unsigned char slot42:1;

    /* 抽選スロットID43
     *   スロットが当選した場合に出現するか
     * Random Slot [43]
     *   Will it appear if the slot is won? */
    unsigned char slot43:1;

    /* 抽選スロットID44
     *   スロットが当選した場合に出現するか
     * Random Slot [44]
     *   Will it appear if the slot is won? */
    unsigned char slot44:1;

    /* 抽選スロットID45
     *   スロットが当選した場合に出現するか
     * Random Slot [45]
     *   Will it appear if the slot is won? */
    unsigned char slot45:1;

    /* 抽選スロットID46
     *   スロットが当選した場合に出現するか
     * Random Slot [46]
     *   Will it appear if the slot is won? */
    unsigned char slot46:1;

    /* 抽選スロットID47
     *   スロットが当選した場合に出現するか
     * Random Slot [47]
     *   Will it appear if the slot is won? */
    unsigned char slot47:1;

    /* 抽選スロットID48
     *   スロットが当選した場合に出現するか
     * Random Slot [48]
     *   Will it appear if the slot is won? */
    unsigned char slot48:1;

    /* 抽選スロットID49
     *   スロットが当選した場合に出現するか
     * Random Slot [49]
     *   Will it appear if the slot is won? */
    unsigned char slot49:1;

    /* 抽選スロットID50
     *   スロットが当選した場合に出現するか
     * Random Slot [50]
     *   Will it appear if the slot is won? */
    unsigned char slot50:1;

    /* 抽選スロットID51
     *   スロットが当選した場合に出現するか
     * Random Slot [51]
     *   Will it appear if the slot is won? */
    unsigned char slot51:1;

    /* 抽選スロットID52
     *   スロットが当選した場合に出現するか
     * Random Slot [52]
     *   Will it appear if the slot is won? */
    unsigned char slot52:1;

    /* 抽選スロットID53
     *   スロットが当選した場合に出現するか
     * Random Slot [53]
     *   Will it appear if the slot is won? */
    unsigned char slot53:1;

    /* 抽選スロットID54
     *   スロットが当選した場合に出現するか
     * Random Slot [54]
     *   Will it appear if the slot is won? */
    unsigned char slot54:1;

    /* 抽選スロットID55
     *   スロットが当選した場合に出現するか
     * Random Slot [55]
     *   Will it appear if the slot is won? */
    unsigned char slot55:1;

    /* 抽選スロットID56
     *   スロットが当選した場合に出現するか
     * Random Slot [56]
     *   Will it appear if the slot is won? */
    unsigned char slot56:1;

    /* 抽選スロットID57
     *   スロットが当選した場合に出現するか
     * Random Slot [57]
     *   Will it appear if the slot is won? */
    unsigned char slot57:1;

    /* 抽選スロットID58
     *   スロットが当選した場合に出現するか
     * Random Slot [58]
     *   Will it appear if the slot is won? */
    unsigned char slot58:1;

    /* 抽選スロットID59
     *   スロットが当選した場合に出現するか
     * Random Slot [59]
     *   Will it appear if the slot is won? */
    unsigned char slot59:1;

    /* 抽選スロットID60
     *   スロットが当選した場合に出現するか
     * Random Slot [60]
     *   Will it appear if the slot is won? */
    unsigned char slot60:1;

    /* 抽選スロットID61
     *   スロットが当選した場合に出現するか
     * Random Slot [61]
     *   Will it appear if the slot is won? */
    unsigned char slot61:1;

    /* 抽選スロットID62
     *   スロットが当選した場合に出現するか
     * Random Slot [62]
     *   Will it appear if the slot is won? */
    unsigned char slot62:1;

    /* 抽選スロットID63
     *   スロットが当選した場合に出現するか
     * Random Slot [63]
     *   Will it appear if the slot is won? */
    unsigned char slot63:1;

    /* 抽選スロットID64
     *   スロットが当選した場合に出現するか
     * Random Slot [64]
     *   Will it appear if the slot is won? */
    unsigned char slot64:1;

    /* 抽選スロットID65
     *   スロットが当選した場合に出現するか
     * Random Slot [65]
     *   Will it appear if the slot is won? */
    unsigned char slot65:1;

    /* 抽選スロットID66
     *   スロットが当選した場合に出現するか
     * Random Slot [66]
     *   Will it appear if the slot is won? */
    unsigned char slot66:1;

    /* 抽選スロットID67
     *   スロットが当選した場合に出現するか
     * Random Slot [67]
     *   Will it appear if the slot is won? */
    unsigned char slot67:1;

    /* 抽選スロットID68
     *   スロットが当選した場合に出現するか
     * Random Slot [68]
     *   Will it appear if the slot is won? */
    unsigned char slot68:1;

    /* 抽選スロットID69
     *   スロットが当選した場合に出現するか
     * Random Slot [69]
     *   Will it appear if the slot is won? */
    unsigned char slot69:1;

    /* 抽選スロットID70
     *   スロットが当選した場合に出現するか
     * Random Slot [70]
     *   Will it appear if the slot is won? */
    unsigned char slot70:1;

    /* 抽選スロットID71
     *   スロットが当選した場合に出現するか
     * Random Slot [71]
     *   Will it appear if the slot is won? */
    unsigned char slot71:1;

    /* 抽選スロットID72
     *   スロットが当選した場合に出現するか
     * Random Slot [72]
     *   Will it appear if the slot is won? */
    unsigned char slot72:1;

    /* 抽選スロットID73
     *   スロットが当選した場合に出現するか
     * Random Slot [73]
     *   Will it appear if the slot is won? */
    unsigned char slot73:1;

    /* 抽選スロットID74
     *   スロットが当選した場合に出現するか
     * Random Slot [74]
     *   Will it appear if the slot is won? */
    unsigned char slot74:1;

    /* 抽選スロットID75
     *   スロットが当選した場合に出現するか
     * Random Slot [75]
     *   Will it appear if the slot is won? */
    unsigned char slot75:1;

    /* 抽選スロットID76
     *   スロットが当選した場合に出現するか
     * Random Slot [76]
     *   Will it appear if the slot is won? */
    unsigned char slot76:1;

    /* 抽選スロットID77
     *   スロットが当選した場合に出現するか
     * Random Slot [77]
     *   Will it appear if the slot is won? */
    unsigned char slot77:1;

    /* 抽選スロットID78
     *   スロットが当選した場合に出現するか
     * Random Slot [78]
     *   Will it appear if the slot is won? */
    unsigned char slot78:1;

    /* 抽選スロットID79
     *   スロットが当選した場合に出現するか
     * Random Slot [79]
     *   Will it appear if the slot is won? */
    unsigned char slot79:1;

    /* 抽選スロットID80
     *   スロットが当選した場合に出現するか
     * Random Slot [80]
     *   Will it appear if the slot is won? */
    unsigned char slot80:1;

    /* 抽選スロットID81
     *   スロットが当選した場合に出現するか
     * Random Slot [81]
     *   Will it appear if the slot is won? */
    unsigned char slot81:1;

    /* 抽選スロットID82
     *   スロットが当選した場合に出現するか
     * Random Slot [82]
     *   Will it appear if the slot is won? */
    unsigned char slot82:1;

    /* 抽選スロットID83
     *   スロットが当選した場合に出現するか
     * Random Slot [83]
     *   Will it appear if the slot is won? */
    unsigned char slot83:1;

    /* 抽選スロットID84
     *   スロットが当選した場合に出現するか
     * Random Slot [84]
     *   Will it appear if the slot is won? */
    unsigned char slot84:1;

    /* 抽選スロットID85
     *   スロットが当選した場合に出現するか
     * Random Slot [85]
     *   Will it appear if the slot is won? */
    unsigned char slot85:1;

    /* 抽選スロットID86
     *   スロットが当選した場合に出現するか
     * Random Slot [86]
     *   Will it appear if the slot is won? */
    unsigned char slot86:1;

    /* 抽選スロットID87
     *   スロットが当選した場合に出現するか
     * Random Slot [87]
     *   Will it appear if the slot is won? */
    unsigned char slot87:1;

    /* 抽選スロットID88
     *   スロットが当選した場合に出現するか
     * Random Slot [88]
     *   Will it appear if the slot is won? */
    unsigned char slot88:1;

    /* 抽選スロットID89
     *   スロットが当選した場合に出現するか
     * Random Slot [89]
     *   Will it appear if the slot is won? */
    unsigned char slot89:1;

    /* 抽選スロットID90
     *   スロットが当選した場合に出現するか
     * Random Slot [90]
     *   Will it appear if the slot is won? */
    unsigned char slot90:1;

    /* 抽選スロットID91
     *   スロットが当選した場合に出現するか
     * Random Slot [91]
     *   Will it appear if the slot is won? */
    unsigned char slot91:1;

    /* 抽選スロットID92
     *   スロットが当選した場合に出現するか
     * Random Slot [92]
     *   Will it appear if the slot is won? */
    unsigned char slot92:1;

    /* 抽選スロットID93
     *   スロットが当選した場合に出現するか
     * Random Slot [93]
     *   Will it appear if the slot is won? */
    unsigned char slot93:1;

    /* 抽選スロットID94
     *   スロットが当選した場合に出現するか
     * Random Slot [94]
     *   Will it appear if the slot is won? */
    unsigned char slot94:1;

    /* 抽選スロットID95
     *   スロットが当選した場合に出現するか
     * Random Slot [95]
     *   Will it appear if the slot is won? */
    unsigned char slot95:1;

    /* 抽選スロットID96
     *   スロットが当選した場合に出現するか
     * Random Slot [96]
     *   Will it appear if the slot is won? */
    unsigned char slot96:1;

    /* 抽選スロットID97
     *   スロットが当選した場合に出現するか
     * Random Slot [97]
     *   Will it appear if the slot is won? */
    unsigned char slot97:1;

    /* 抽選スロットID98
     *   スロットが当選した場合に出現するか
     * Random Slot [98]
     *   Will it appear if the slot is won? */
    unsigned char slot98:1;

    /* 抽選スロットID99
     *   スロットが当選した場合に出現するか
     * Random Slot [99]
     *   Will it appear if the slot is won? */
    unsigned char slot99:1;

    char pad:4;
} er_random_appear_param_t;
